<h1>New hori</h1>

<%= form_tag('/hori', :method => :post, :class => 'form-inline') do |f| %>
  <%= render 'form' %>
  <div class="actions">
    <%= submit_tag "追加", :class => 'btn btn-primary btn-sm' %>
  </div>
<% end %>

<%= link_to 'Back', "/hori/index/#{ERB::Util.url_encode(@path)}" %>
